import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.zerovault.app',
  appName: 'ZeroVault',
  webDir: 'dist/zerovault/browser'
};

export default config;
